'use strict';

var GetIntrinsic = require('get-intrinsic');

var $TypeError = GetIntrinsic('%TypeError%');

var DefineOwnProperty = require('es-abstract/helpers/DefineOwnProperty');

var FromPropertyDescriptor = require('es-abstract/2018/FromPropertyDescriptor');
var IsDataDescriptor = require('es-abstract/2018/IsDataDescriptor');
var IsPropertyKey = require('es-abstract/2018/IsPropertyKey');
var SameValue = require('es-abstract/2018/SameValue');
var Type = require('es-abstract/2018/Type');

// https://262.ecma-international.org/6.0/#sec-createmethodproperty

module.exports = function CreateMethodProperty(O, P, V) {
	if (Type(O) !== 'Object') {
		throw new $TypeError('Assertion failed: Type(O) is not Object');
	}

	if (!IsPropertyKey(P)) {
		throw new $TypeError('Assertion failed: IsPropertyKey(P) is not true');
	}

	var newDesc = {
		'[[Configurable]]': true,
		'[[Enumerable]]': false,
		'[[Value]]': V,
		'[[Writable]]': true
	};
	return DefineOwnProperty(
		IsDataDescriptor,
		SameValue,
		FromPropertyDescriptor,
		O,
		P,
		newDesc
	);
};
